Efficient Solution : O(N)
------------------------
// n is size of array

void zeroesEnd(int array[],int n){
    int count=0;
    for(int i=0;i<n;i++){
        if(array[i]!=0){
            array[res]=array[i];
            array[i]=0;
            count++;
        }
    }
}
