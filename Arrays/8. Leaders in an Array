Efficient Solution : O(N)
------------------------

void leaders(int arr[],int n){
  
    int curr_leadeer= arr[n-1];
    cout<<(curr_leader)<<" ";

    for(int i=n-2;i>=0;i--)
    {   if(curr_leader<arr[i])  
            curr_leader = arr[i];
              print(curr_leader);
    } 

}
