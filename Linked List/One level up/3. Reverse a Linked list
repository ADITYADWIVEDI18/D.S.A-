Method -1
---------
node *reverse (node *head){

  node *temp =head;
  node *prev =NULL;

  while(curr!=NULL){
    node *next = curr->next;
    curr->next = prev;
    prev = curr;
    curr = next;
  }
return prev;

}
